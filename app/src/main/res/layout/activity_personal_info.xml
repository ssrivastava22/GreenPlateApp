<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/tools">

    <data>

        <variable
            name="personalInfoViewModel"
            type="com.example.greenplate.viewmodels.PersonalInfoViewModel"/>
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp">

        <TextView
            android:id="@+id/titleTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Personal Information"
            android:textSize="30sp"
            android:textColor="@color/black"
            android:layout_marginTop="16dp"
            android:gravity="center" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/titleTextView">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <EditText
                    android:id="@+id/editHeightText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:autofillHints="Height"
                    android:padding="16dp"
                    android:text="@={personalInfoViewModel.userInfo.height}" />

                <EditText
                    android:id="@+id/editWeightText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:autofillHints="Weight"
                    android:padding="16dp"
                    android:text="@={personalInfoViewModel.userInfo.weight}" />

                <EditText
                    android:id="@+id/editGenderText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:autofillHints="Gender"
                    android:padding="16dp"
                    android:text="@={personalInfoViewModel.userInfo.gender}" />


                <Button
                    android:id="@+id/enterUserInfoButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:onClick="@{() -> personalInfoViewModel.saveUserInfo()}"
                    android:text="ENTER" />

            </LinearLayout>
        </ScrollView>
    </RelativeLayout>
</layout>
